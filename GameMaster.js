function GameMaster(_gameObserver){

	var GameObserver = _gameObserver;

	/*

		
		conditions.inputCodes = []
		conditions.objectArray = []

		objectArray[i] = {
			'receiver':{
				'libraryTypes':[],
				'objectTypes':[],
				'objectLabels':[]
			}
			'propertyNames':[],
			'operative': '<,>,>=,<=,==,!=,'
			'conditionValue': value relative to the operative opposite the property for the condition;
			}
		}

		inputCodes[i] = {
			'inputCodes':[],
			'operative':'<,>,>=,<=,==,!=,'
			'conditionValue':
		}
	*/

	function CreateConditions(params)
	{

	}
	function BondConditions(params)
	{

	}
	function CreateRules(params)
	{
		var inputArray = params.inputCodes;
		var gameObjectPropArray = params.gameObjectProp;
	}

	function CreateNewStates()
	{

	}

}